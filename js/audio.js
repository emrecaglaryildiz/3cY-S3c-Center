/**
 * SEC-CENTER - Audio SFX Module
 * Sesler kullanıcı talebi üzerine devre dışı bırakıldı.
 */

window.SecAudio = (function() {
  function init() {}
  function playClick() {}
  function playAlert() {}
  function playTyping() {}

  return {
    init,
    playClick,
    playAlert,
    playTyping
  };
})();
